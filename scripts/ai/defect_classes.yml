# Defect-class registry
#
# Every defect class found on a PR becomes a grep-able rule here, so it can never
# silently recur. This is what turns "sweep the class" from a judgement call into a
# deterministic check — the judgement is what failed nine times on PR #317.
#
# Consumed by scripts/ai/defect_scan.py (and, later, the Tier E stage of
# scripts/ai/pre_push_audit.py — see todo 080).
#
# ---------------------------------------------------------------------------
# SCOPE BOUNDARY — what does NOT belong here
# ---------------------------------------------------------------------------
# This registry holds classes learned from real review findings. It deliberately
# does NOT duplicate:
#   * AGENTS.md "DO NOT" guards        -> gate Tier B
#   * .cursor/rules/*.mdc rules        -> gate Tier C
#   * the doc-consistency change map   -> gate Tier D
# Keep those in their own tiers so a rule has exactly one home.
#
# ---------------------------------------------------------------------------
# SCHEMA
# ---------------------------------------------------------------------------
# id            kebab-case, stable. Cited in review replies and commit messages.
# description   one line: what the defect IS, not how to find it.
# source        where the class was learned (PR + the rounds it recurred in).
# severity      blocker | important | minor  (REVIEW.md rubric)
# detection     how the scanner treats matches:
#                 mechanical    - ANY match is a failure unless allow-listed.
#                                 Use only when the pattern is precise.
#                 baseline      - current matches are PINNED in allow_list.
#                                 A NEW match fails. This is how a heuristic
#                                 becomes enforceable: you cannot add instance
#                                 N+1 without adjudicating it.
#                 review-prompt - not reliably greppable. Emits a REQUIRED
#                                 checklist item when paths change; the gate
#                                 fails if unacknowledged. Never silently skipped.
# paths         glob(s) the class applies to. The scanner walks the WHOLE matching
#               surface, not just changed hunks — PR #317's round-9 misses were in
#               files the change never touched.
# patterns      Python re syntax, applied per line unless multiline: true.
# allow_list    known-good matches. EVERY entry needs a reason. An allow_list entry
#               without a reason is itself a finding.
# remediation   what the author should actually do. A gate that fails without
#               telling you the fix gets bypassed with --no-verify.
#
# ---------------------------------------------------------------------------

version: 1

classes:
  # =========================================================================
  # Learned from PR #317 (refresh-dt-preview-no-forward-button), 9 rounds.
  # =========================================================================

  - id: raw-debug-in-user-output
    description: >
      Raw serialized data (JSON.stringify, char codes, stack text) reaching a
      user-facing surface — toast, banner, tile, tooltip.
    source: "PR #317 round 9"
    severity: important
    detection: mechanical
    paths:
      - "force-app/**/lwc/**/*.js"
      - "unpackaged/**/lwc/**/*.js"
    patterns:
      # a serializer feeding something named like user-facing text
      - '(?:message|title|label|banner|toast|subtitle|note|detail|summary)\w*\s*[:=][^;\n]*JSON\.stringify'
      # …or a serializer inside a toast dispatch
      - 'ShowToastEvent\([^)]*JSON\.stringify'
      - 'charCodeAt\([^)]*\)[^;\n]*(?:message|label|title)'
    allow_list: []
    remediation: >
      Render a human sentence. If the raw payload is genuinely needed for support,
      log it to console and surface a short scoped message. See
      .cursor/rules/lwc-components.mdc.

  - id: absent-null-into-safe-predicate
    description: >
      A null meaning "absent/unknown" flowing into a predicate whose false branch
      means "safe" — so missing data reads as a clean result. Fail closed instead.
    source: "PR #317 round 3"
    severity: blocker
    detection: baseline
    paths:
      - "force-app/**/lwc/**/*.js"
      - "unpackaged/**/lwc/**/*.js"
      - "force-app/**/classes/*.cls"
      - "unpackaged/**/classes/*.cls"
    patterns:
      - '\b(?:is|has)(?:Fresh|Valid|Safe|Ok|Ready|Clean|Current)\w*\s*=\s*[^;\n]*(?:==|!=)\s*(?:null|NULL)'
      - '\b(?:fresh|valid|safe|ok|ready)\w*\s*=\s*[^;\n]*\?\?\s*true'
    allow_list: []
    remediation: >
      Make absent its own state. A probe that could not read the source must not
      report the reassuring answer — return unknown and say so in the UI.

  - id: ordering-tie-claim
    description: >
      A strict comparison backing a user-facing claim of ordering ("after the last
      full sync", "later than"), where equal timestamps are possible and the tie
      makes the claim false.
    source: "PR #317 rounds 3, 9"
    severity: important
    detection: baseline
    paths:
      - "force-app/**/lwc/**/*.js"
      - "unpackaged/**/lwc/**/*.js"
      - "force-app/**/classes/*.cls"
      - "unpackaged/**/classes/*.cls"
    patterns:
      - '(?:LastModifiedDate|SystemModstamp|CreatedDate|lastSync|lastRefresh|lastRun|LastRefreshDate)\w*\s*[<>](?!=)'
      - '[<>](?!=)\s*(?:LastModifiedDate|SystemModstamp|CreatedDate|lastSync|lastRefresh|lastRun)'
      - '\.getTime\(\)\s*[<>](?!=)'
    exclude_paths:
      - "**/*Test.cls"
    allow_list:
      - file: "unpackaged/post_utils/classes/RLM_DecisionTableManagerController.cls"
        match: "lastSyncDate <"
        reason: >
          Backs the word "predates", which means strictly before. A tie here should
          NOT warn, so strict < matches the sentence the UI shows. Verified against
          neverBuiltInThisOrgReason().
    remediation: >
      Decide explicitly what a tie means and use >= / <= to match the sentence the
      UI shows. Equal timestamps are common — same-transaction writes share one.

  - id: visibility-scope-missing
    description: >
      A user-facing string built from USER_MODE-filtered probe data that asserts a
      state of the SOURCE ("nothing matched", "all tables") without scoping the
      claim to what the running user can actually SEE.
    source: "PR #317 rounds 4, 5, 9 — each in a surface the prior sweep did not reach"
    severity: blocker
    detection: baseline
    ignore_comment_lines: true
    paths:
      - "force-app/**/lwc/**/*.js"
      - "unpackaged/**/lwc/**/*.js"
      - "force-app/**/lwc/**/*.html"
      - "unpackaged/**/lwc/**/*.html"
    patterns:
      # An absolute claim about a source set, NOT followed by a scoping qualifier.
      # The negative lookahead is the whole point: "matched nothing visible" is the
      # FIXED form and must not fire, or the gate cries wolf on its own remedy.
      - "(?:matched nothing|nothing matched|no records|no rows|none found)(?![^'\"`\\n]*(?:visible|you can see|your access|of the ones))"
    allow_list:
      - file: "unpackaged/post_utils/lwc/rlmDecisionTableManager/rlmDecisionTableManager.html"
        match: "Refreshing by usage type updates every table"
        reason: >
          Describes what the refresh action does, not what a USER_MODE probe saw.
          The statement is true: refreshing by usage type does update every table
          sharing the value, including ones the running user cannot see.
    remediation: >
      Scope the sentence to visibility ("nothing VISIBLE matched") or carry the
      qualification the detail view already carries. A USER_MODE probe cannot speak
      for records the user cannot read.

  - id: scoped-truth-stated-universally
    description: >
      A summary surface (tile, tooltip, table row, header count) dropping a
      qualification that its own detail view carries — same fact, two scopes.
    source: "PR #317 rounds 7, 9"
    severity: important
    detection: review-prompt
    paths:
      - "force-app/**/lwc/**/*"
      - "unpackaged/**/lwc/**/*"
    patterns: []
    remediation: >
      For every summary string, find the detail string it summarises and confirm the
      qualifications match. If the detail says "visible" or "of the ones checked",
      the summary must not drop it.

  - id: partial-outcome-bucket-dropped
    description: >
      An early return reporting one outcome bucket while silently discarding another
      (e.g. reports failures, drops skipped; reports refreshed, drops unchanged).
    source: "PR #317 rounds 3, 9"
    severity: important
    detection: review-prompt
    paths:
      - "force-app/**/lwc/**/*.js"
      - "unpackaged/**/lwc/**/*.js"
      - "force-app/**/classes/*.cls"
      - "unpackaged/**/classes/*.cls"
    patterns: []
    remediation: >
      Enumerate every bucket the function can produce and confirm each early return
      accounts for all of them. Silent truncation reads as "covered everything".

  - id: doc-vs-code-drift
    description: >
      A comment, skill, or doc asserting behaviour the code no longer has — most
      often left behind by a fix that changed the behaviour but not its description.
    source: "PR #317 rounds 4, 7"
    severity: important
    detection: review-prompt
    paths:
      - "**/*.md"
      - "force-app/**/*.js"
      - "unpackaged/**/*.js"
      - "**/*.cls"
    patterns: []
    remediation: >
      For every behavioural change in the diff, grep the repo for prose describing
      the OLD behaviour — including comments in the same file and skills that cite it.

  # =========================================================================
  # audit-review/SKILL.md recurring checklist. These predate #317 and are the
  # classes the internal Salesforce audit re-raises.
  # =========================================================================

  - id: soql-missing-user-mode
    description: >
      Entry-reachable SOQL or DML in a controller running without USER_MODE /
      as user — the #1 audit finding class.
    source: "audit-review/SKILL.md; PR #203 (swept 5 flagged -> 20 real)"
    severity: blocker
    detection: baseline
    paths:
      - "force-app/**/classes/*.cls"
      - "unpackaged/**/classes/*.cls"
    patterns:
      - '\[\s*SELECT\b(?![^\]]*(?:WITH USER_MODE|WITH SECURITY_ENFORCED))[^\]]*\]'
    # The pattern needs the closing `]`, so a line-based scan only ever sees
    # single-line queries. 265 of this repo's 370 SOQL literals span lines — a
    # share the Apex reformat on this branch increased, since Prettier splits
    # long queries. Without this the class silently covered 28% of its surface.
    multiline: true
    exclude_paths:
      # Test classes run in system mode by design; a test asserting on seeded data
      # is not an audit finding.
      - "**/*Test.cls"
    # NOTE: the baseline below is PRE-EXISTING and NOT YET ADJUDICATED. Pinning it
    # is what lets the gate do its real job today — block instance N+1 — without
    # pretending the backlog is clean. See the registry README note in todo 080.
    allow_list:
      - file: "force-app/main/default/classes/RLM_AssetInfoUtility.cls"
        match: "[ SELECT Id, ServiceDate, EndDate, OrderAction.Type, ProductSellingModel.SellingModelType, ProductSellingModel.PricingTermUnit, Product2.Name FROM OrderItem WHERE Id IN :orderItemIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "force-app/main/default/classes/RLM_AssetInfoUtility.cls"
        match: "[ SELECT Id, AssetId, Contract.ContractTerm FROM AssetContractRelationship WHERE AssetId IN :assetIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "force-app/main/default/classes/RLM_DFOTenantProvisioningCallout.cls"
        match: "[ SELECT Id, OrderId FROM OrderItem WHERE Id = :inputRecordId LIMIT 1 ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "force-app/main/default/classes/RLM_DFOTenantProvisioningCallout.cls"
        match: "[ SELECT Id, SourceLineItemId, FulfilmentOrderLineId FROM FulfillmentLineSourceRel WHERE SourceLineItem.Type = 'OrderItem' AND FulfilmentOrderLineId = :inputRecordId LIMIT 1 ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "force-app/main/default/classes/RLM_DFOTenantProvisioningCallout.cls"
        match: "[ SELECT Id, OrderId FROM OrderItem WHERE Id = :lineSourceRel.SourceLineItemId LIMIT 1 ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "force-app/main/default/classes/RLM_OrderItemContractingUtility.cls"
        match: "[ SELECT Id, EffectiveDate, ( SELECT Id, ServiceDate, EndDate, ProductSellingModel.SellingModelType FROM OrderItems WHERE ProductSellingModel.SellingModelType != 'OneTime' AND EndDate != NULL ORDER BY EndDate DESC LIMIT 1 ), (SELECT Id, Type FROM OrderActions WHERE Type IN ('Add', 'Amend', 'Renew')) FROM Order WHERE Id IN :inputOrderIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "force-app/main/default/classes/RLM_QuoteModelUtility.cls"
        match: "[ SELECT QuoteLineItemId, AttributeName, AttributeValue FROM QuoteLineItemAttribute WHERE QuoteLineItemId = :ql.Id ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "force-app/main/default/classes/RLM_QuoteModelUtility.cls"
        match: "[ SELECT AssociatedQuoteLineId, AssociatedQuoteLine.ProductSellingModelId, AssociatedQuoteLine.Product2Id, AssociatedQuoteLinePricing, MainQuoteLineId, MainQuoteLine.ProductSellingModelId, MainQuoteLine.Product2Id FROM QuoteLineRelationship WHERE AssociatedQuoteLineId = :ql.Id ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "force-app/main/default/classes/RLM_QuoteModelUtility.cls"
        match: "[ SELECT Id, Pricebook2Id, CurrencyIsoCode, CreatedDate FROM Quote WHERE Id IN :quoteId LIMIT 1 ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "force-app/main/default/classes/RLM_QuoteModelUtility.cls"
        match: "[ SELECT QuoteId, ProductSellingModelId, SubscriptionTerm, Product2Id, ListPrice, Discount, DiscountAmount, StartDate, EndDate, LineNumber, Quantity, PeriodBoundary, PeriodBoundaryDay, PeriodBoundaryStartMonth, SellingModelType, ProrationPolicy.ArePartialPeriodsAllowed, PricingTermUnit FROM QuoteLineItem WHERE QuoteId = :quoteId ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_AddProductToQuoteService.cls"
        match: "[SELECT Id, Name FROM Product2 WHERE Id = :productId LIMIT 1]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_AddProductToQuoteService.cls"
        match: "[ SELECT Id, ProductSellingModelId, ProductSellingModel.Name, IsDefault FROM ProductSellingModelOption WHERE Product2Id = :productId ORDER BY IsDefault DESC ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_AddProductToQuoteService.cls"
        match: "[ SELECT Id FROM QuoteLineItem WHERE QuoteId = :quoteId AND Product2Id = :productId ORDER BY CreatedDate DESC, LineNumber DESC LIMIT 1 ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_ApplyQuoteLineDiscountsService.cls"
        match: "[ SELECT Id, Product2.Name, Quantity, UnitPrice, NetUnitPrice, ListPrice, LineNumber FROM QuoteLineItem WHERE QuoteId = :quoteId AND Id IN :requested ORDER BY LineNumber ASC ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_ApplyQuoteLineDiscountsService.cls"
        match: "[ SELECT Id, Product2.Name, Quantity, UnitPrice, NetUnitPrice, ListPrice, LineNumber FROM QuoteLineItem WHERE QuoteId = :quoteId ORDER BY LineNumber ASC, Product2.Name ASC LIMIT 200 ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_AttributeResolver.cls"
        match: "[ SELECT Product2Id, Product2.Name, Product2.BasedOnId FROM QuoteLineItem WHERE Id = :quoteLineItemId LIMIT 1 ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_AttributeResolver.cls"
        match: "[ SELECT AttributeDefinitionId, IsRequired, IsPriceImpacting FROM ProductClassificationAttr WHERE ProductClassificationId = :classificationId AND Status = 'Active' AND IsReadOnly = FALSE AND IsHidden = FALSE ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_AttributeResolver.cls"
        match: "[ SELECT AttributeName, AttributeValue, IsPriceImpacting, AttributeDefinitionId FROM QuoteLineItemAttribute WHERE QuoteLineItemId = :quoteLineItemId ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_AttributeResolver.cls"
        match: "[ SELECT Id, Name, DeveloperName, DataType, PicklistId FROM AttributeDefinition WHERE Id IN :attrDefIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_AttributeResolver.cls"
        match: "[ SELECT Name, Code, PicklistId FROM AttributePicklistValue WHERE PicklistId IN :picklistIds ORDER BY Name ASC ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_ConfigServiceUtils.cls"
        match: "[ SELECT Id, Product2Id, Product2.Name, Quantity, UnitPrice, NetUnitPrice, ListPrice, LineNumber, ProductSellingModel.SellingModelType FROM QuoteLineItem WHERE QuoteId = :quoteId ORDER BY LineNumber ASC LIMIT 200 ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_ProductAttributeReadService.cls"
        match: "[ SELECT AttributeName, AttributeValue FROM QuoteLineItemAttribute WHERE QuoteLineItemId = :qliId ORDER BY AttributeName ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_ProductAttributeSaveService.cls"
        match: "[ SELECT Id, AttributeDefinition.DeveloperName, AttributeDefinition.DataType, AttributeDefinition.PicklistId FROM QuoteLineItemAttribute WHERE QuoteLineItemId = :quoteLineItemId ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_ProductAttributeSaveService.cls"
        match: "[ SELECT Id, Name, Code, Value, PicklistId FROM AttributePicklistValue WHERE PicklistId IN :picklistIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_ProductAttributeSaveService.cls"
        match: "[ SELECT AttributeDefinition.DeveloperName, AttributeValue FROM QuoteLineItemAttribute WHERE QuoteLineItemId = :quoteLineItemId AND AttributeDefinition.DeveloperName IN :submittedDevNames ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_ProductAttributeSaveService.cls"
        match: "[ SELECT COUNT() FROM QuoteLineItem WHERE Id = :quoteLineItemId AND QuoteId = :quoteId ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_QuoteDemoSummaryService.cls"
        match: "[ SELECT Id, Name FROM Quote WHERE Id = :quoteId LIMIT 1 ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_QuoteDemoSummaryService.cls"
        match: "[ SELECT Id, Product2.Name, Quantity, NetUnitPrice, UnitPrice, NetTotalPrice, TotalPrice, LineNumber FROM QuoteLineItem WHERE QuoteId = :quoteId ORDER BY LineNumber ASC, Product2.Name ASC LIMIT 200 ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_QuoteLineItemLookupService.cls"
        match: "[ SELECT Id, Product2Id, Product2.Name, Quantity, UnitPrice, NetUnitPrice, TotalPrice, NetTotalPrice FROM QuoteLineItem WHERE QuoteId = :quoteId ORDER BY LineNumber ASC, Product2.Name ASC LIMIT :MAX_CANDIDATES ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_UpdateRecordFieldsService.cls"
        match: "[ SELECT Id, Name, Status, ExpirationDate FROM Quote WHERE Id = :quoteId LIMIT 1 ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_UpdateRecordFieldsService.cls"
        match: "[SELECT QuoteId FROM QuoteLineItem WHERE Id IN :lineIds]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_UpdateRecordFieldsService.cls"
        match: "[ SELECT Id, Product2.Name, Quantity, UnitPrice, LineNumber, ProductSellingModel.SellingModelType FROM QuoteLineItem WHERE QuoteId = :quoteId AND Id IN :requested ORDER BY LineNumber ASC ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_agents/classes/RLM_AI_UpdateRecordFieldsService.cls"
        match: "[ SELECT Id, Product2.Name, Quantity, UnitPrice, LineNumber, ProductSellingModel.SellingModelType FROM QuoteLineItem WHERE QuoteId = :quoteId ORDER BY LineNumber ASC, Product2.Name ASC LIMIT 200 ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_constraints/classes/UTIL_ConfigHelper.cls"
        match: "[SELECT Id, ProductCode, Name FROM Product2 WHERE Id = :productId LIMIT 1]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_docgen/classes/RLM_DocumentGenerationCreate.cls"
        match: "[SELECT Id, TokenMappingMethodType FROM DocumentTemplate WHERE Id IN :templateIds]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_docgen/classes/RLM_ReturnPDFDocument.cls"
        match: "[ SELECT Id, FileType, ContentDocumentId FROM ContentVersion WHERE Id IN :contentVersionIds AND FileType = 'PDF' LIMIT 1 ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_ARC_AssetValidator.cls"
        match: "[ SELECT Id, Name, Product2.Name, LifecycleStartDate, LifecycleEndDate FROM Asset WHERE Id IN :allBlockedIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_ARC_AssetValidator.cls"
        match: "[ SELECT AssetId, RelatedAssetId FROM AssetRelationship WHERE AssetId IN :allInputIds OR RelatedAssetId IN :allInputIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_ARC_AssetValidator.cls"
        match: "[ SELECT AssetId FROM AssetStatePeriod WHERE AssetId IN :assetIds AND RampIdentifier != NULL ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_ARC_AssetValidator.cls"
        match: "[ SELECT Id, Product2Id, LifecycleEndDate FROM Asset WHERE Id IN :assetIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_ARC_AssetValidator.cls"
        match: "[ SELECT ProductId FROM ProductUsageResource WHERE ProductId IN :productIds AND Status = 'Active' ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_ARC_AssetValidator.cls"
        match: "[ SELECT Product2Id FROM PricebookEntry WHERE Product2Id IN :productIds AND IsDerived = TRUE ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM Order WHERE AccountId = :accountId ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM OrderItem WHERE Order.AccountId = :accountId ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM Invoice WHERE BillingAccountId = :accountId ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM OrderSummary WHERE AccountId = :accountId ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM FulfillmentOrder WHERE AccountId = :accountId OR OrderId IN :this.orderIds OR InvoiceId IN :invoiceIds OR OrderSummaryId IN :summaryIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, SourceIdentifier, SourceType FROM FulfillmentPlan WHERE SourceIdentifier IN :this.sourceIdentifiers AND (SourceType = 'StandardOrder' OR SourceType = 'OrderItem') ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM FulfillmentStep WHERE FulfillmentPlanId IN :this.fulfillmentPlanIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM FulfillmentAsset WHERE AccountId = :accountId ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM FulfillmentOrderLineItem WHERE FulfillmentOrderId IN :this.fulfillmentOrderIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM FulfillmentLineSourceRel WHERE SourceLineItemId IN :this.orderItemIds OR FulfilmentOrderLineId IN :this.fulfillmentOrderLineItemIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM Quote WHERE QuoteAccountId = :acctIdConverted]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM Opportunity WHERE AccountId = :accountIdConverted]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM DocumentGenerationProcess WHERE ReferenceObject IN :quoteIdList ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT ContentDocumentId FROM ContentDocumentLink WHERE LinkedEntityId IN :chunk ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT ContentDocumentId, LinkedEntityId, ShareType FROM ContentDocumentLink WHERE ContentDocumentId IN :chunk ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM ContentDocument WHERE Id IN :contentDocIds]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM OrderItem WHERE OrderId IN :orderIds]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name, ParentId, Parent.Type, Parent.Name FROM UsageEntitlementBucket WHERE TransactionUsageEntitlementId IN :entitlementIds OR ParentId IN :entitlementIds OR Id IN :entitlementIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name, ParentId, Parent.Type, Parent.Name FROM UsageEntitlementBucket WHERE ParentId IN :entitlementIds OR Id IN :entitlementIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name, ParentId, Parent.Type, Parent.Name FROM UsageEntitlementBucket WHERE ParentId IN :currentLevelIds AND Id NOT IN :allBucketIds // Avoid infinite loops ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM UsageEntitlementBucket WHERE Id IN :toDelete]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM UsageEntitlementBucket WHERE Id IN :topLevelParentIds]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name FROM UsageEntitlementBucket WHERE Id IN :allBucketIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name, TransactionUsageEntitlementId, ParentId, Parent.Type, Parent.Name FROM UsageEntitlementBucket WHERE TransactionUsageEntitlementId IN :entitlementIds OR ParentId IN :entitlementIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name, TransactionUsageEntitlementId, ParentId, Parent.Type, Parent.Name FROM UsageEntitlementBucket WHERE ParentId IN :entitlementIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name, TransactionUsageEntitlementId, ParentId, Parent.Type, Parent.Name FROM UsageEntitlementBucket WHERE ParentId IN :currentLevelIds AND Id NOT IN :allBucketIds // Don't include already found buckets ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name FROM TransactionUsageEntitlement WHERE Id IN :tueIdsToDelete ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name FROM UsageEntitlementBucket WHERE TransactionUsageEntitlementId IN :entitlementIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name FROM UsageEntitlementBucket WHERE ParentId IN :entitlementIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name FROM TransactionUsageEntitlement WHERE AccountId = :accountIdConverted ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name, TransactionUsageEntitlementId, ParentId, Parent.Type, Parent.Name FROM UsageEntitlementBucket WHERE TransactionUsageEntitlementId IN :tueIds OR ParentId IN :tueIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name FROM TransactionUsageEntitlement WHERE Id IN :tueIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name, TransactionUsageEntitlementId FROM UsageEntitlementBucket WHERE TransactionUsageEntitlementId IN :tueIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id, Name FROM Asset WHERE AccountId = :accountId]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name, AccountId FROM TransactionUsageEntitlement WHERE AccountId = :accountId ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name, TransactionUsageEntitlementId, ParentId FROM UsageEntitlementBucket WHERE TransactionUsageEntitlementId IN :allTueIds OR ParentId IN :allTueIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name FROM TransactionUsageEntitlement WHERE Id IN :allTueIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name, AccountId FROM TransactionUsageEntitlement WHERE AccountId = :accountId ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name FROM UsageEntitlementAccount WHERE AccountId = :accountIdConverted ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name, TransactionUsageEntitlementId, ParentId, Parent.Type, Parent.Name FROM UsageEntitlementBucket WHERE ParentId IN :ueaIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name FROM UsageEntitlementAccount WHERE Id IN :ueaIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name, ParentId FROM UsageEntitlementBucket WHERE ParentId IN :ueaIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM Asset WHERE AccountId = :accountIdConverted]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM Contract WHERE AccountId = :accountIdConverted]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM TransactionJournal WHERE AccountId = :accountId ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM Asset WHERE AccountId = :accountId]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM UsageEntitlementEntry WHERE TransactionUsageEntitlement.Account.Id = :accountId LIMIT :TEARDOWN_BATCH ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM UsageBillingPeriodItem WHERE AccountId = :accountId OR AssetId IN :assetIds LIMIT :TEARDOWN_BATCH ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM UsageSummary WHERE AccountId = :accountId OR AssetId IN :assetIds LIMIT :TEARDOWN_BATCH ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM UsageRatableSummary WHERE AccountId = :accountId OR AssetId IN :assetIds LIMIT :TEARDOWN_BATCH ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM TransactionJournal WHERE AccountId = :accountId LIMIT :TEARDOWN_BATCH ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM UsageEntitlementBucket WHERE TransactionUsageEntitlement.Account.Id = :accountId LIMIT :TEARDOWN_BATCH ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM TransactionUsageEntitlement WHERE AccountId = :accountId LIMIT :TEARDOWN_BATCH ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM UsageEntitlementAccount WHERE AccountId = :accountId LIMIT :TEARDOWN_BATCH ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM UsageCmtAssetRelatedObj WHERE AssetId IN :assetIds OR RelatedObjectId IN :assetIds LIMIT :TEARDOWN_BATCH ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM AssetRateCardEntry WHERE Asset.AccountId = :accountId]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM BillingScheduleGroup WHERE BillingAccountId = :accountId ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM Contract WHERE AccountId = :accountId]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM AssetRelationship WHERE AssetId IN :assetIds]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id, Name FROM Asset WHERE AccountId = :accountId]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM UsageSummary WHERE AssetId IN :assetIds]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM UsageSummary WHERE AccountId = :acctId]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM UsageRatableSummary WHERE AssetId IN :assetIds]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM TransactionUsageEntitlement WHERE AssetId IN :assetIds]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM TransactionUsageEntitlement WHERE AccountId = :acctId]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM UsageEntitlementAccount WHERE AccountId = :acctId]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM UsageEntitlementBucket WHERE TransactionUsageEntitlementId IN :allTueIds OR ParentId IN :bucketSearchIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM UsageEntitlementBucket WHERE ParentId IN :currentLevelIds AND Id NOT IN :allBucketIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM UsageEntitlementBucket WHERE Id IN :allBucketIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM TransactionUsageEntitlement WHERE Id IN :allTueIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM UsageEntitlementAccount WHERE Id IN :allUeaIds]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name FROM TransactionUsageEntitlement WHERE Id IN :tueIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name FROM UsageEntitlementAccount WHERE Id IN :ueaIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name, AccountId, AssetId, GrantBindingTargetId FROM TransactionUsageEntitlement WHERE Name IN :specificTUENames ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name, AccountId FROM UsageEntitlementAccount WHERE Name IN :specificUEANames ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name FROM TransactionUsageEntitlement WHERE Name IN :specificTUENames ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM TransactionUsageEntitlement WHERE Id IN :tueIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id, Name FROM UsageEntitlementAccount WHERE Name IN :specificUEANames ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM UsageEntitlementAccount WHERE Id IN :ueaIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM Asset WHERE AccountId = :accountIdConverted]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id, Status FROM Invoice WHERE BillingAccountId = :accountId]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM FulfillmentLineSourceRel WHERE SourceLineItemId IN :orderData.orderItemIds OR FulfilmentOrderLineId IN :orderData.fulfillmentOrderLineItemIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM FulfillmentStepSource WHERE StepId IN :orderData.fulfillmentStepIds OR SourceLineItemId IN :orderData.orderItemIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM AssetFulfillmentDecomp WHERE FulfillmentSourceAssetId IN :orderData.fulfillmentAssetIds OR FulfillmentTargetAssetId IN :orderData.fulfillmentAssetIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM BindingObjUsageRsrcPlcy WHERE BindingObjectId = :accountId ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM Asset WHERE AccountId = :accountId]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM BindingObjUsageRsrcPlcy WHERE BindingObjectId IN :bindAssetIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[SELECT Id FROM Contract WHERE AccountId = :accountId]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM BindingObjUsageRsrcPlcy WHERE BindingObjectId IN :bindContractIds ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM BindingObjectRateCardEntry WHERE BindingObjectId = :accountId ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_AccountUtilities.cls"
        match: "[ SELECT Id FROM BindingObjectRateCardEntry WHERE BindingObjectId IN (SELECT Id FROM Contract WHERE AccountId = :accountId) ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_ExpressionSetManagerController.cls"
        match: "[ SELECT Id, Status, ExtendedStatus, NumberOfErrors FROM AsyncApexJob WHERE Id = :jobId AND CreatedById = :currentUserId AND ApexClass.Name = 'RLM_ExpressionSetManagerController' LIMIT 1 ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_UsageUploaderController.cls"
        match: "[ SELECT Id, Name, Product2.Name, Product2.UsageModelType FROM Asset WHERE AccountId = :accountId AND LifecycleStartDate <= TODAY AND LifecycleEndDate >= TODAY AND Product2.UsageModelType = 'Anchor' ORDER BY Name ASC ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_UsageUploaderController.cls"
        match: "[ SELECT Id, Product2Id FROM Asset WHERE Id = :assetId LIMIT 1 ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_UsageUploaderController.cls"
        match: "[ SELECT Id, UsageResourceId, UsageResource.Name, UsageResource.DefaultUnitOfMeasureId, UsageResource.DefaultUnitOfMeasure.Name, UsageResource.DefaultUnitOfMeasure.UnitCode, UsageResource.UnitOfMeasureClassId FROM ProductUsageResource WHERE ProductId = :ast.Product2Id AND Status = 'Active' ORDER BY UsageResource.Name ASC ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_UsageUploaderController.cls"
        match: "[ SELECT Id, Name, UnitCode, UnitOfMeasureClassId FROM UnitOfMeasure WHERE UnitOfMeasureClassId IN :uomClassIds AND Status = 'Active' ORDER BY Name ASC ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_UsageUploaderController.cls"
        match: "[ SELECT Id, AccountId FROM Asset WHERE Id = :assetId LIMIT 1 ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_UsageUploaderController.cls"
        match: "[ SELECT Id, DefaultUnitOfMeasureId, UnitOfMeasureClassId FROM UsageResource WHERE Id = :usageResourceId LIMIT 1 ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_UsageUploaderController.cls"
        match: "[ SELECT Id, Status, UnitOfMeasureClassId FROM UnitOfMeasure WHERE Id = :unitOfMeasureId AND UnitOfMeasureClassId = :ur.UnitOfMeasureClassId LIMIT 1 ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_UsageUploaderController.cls"
        match: "[ SELECT Id, AccountId FROM Asset WHERE Id = :assetId LIMIT 1 ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_UsageUploaderController.cls"
        match: "[ SELECT Id, AssetId, UsageResourceId, NegotiatedRate, RateCardEntryId, RateCardEntry.Rate, RateCardEntry.RateUnitOfMeasureName, RateCardEntry.RateCardType, RateCardId, RateCard.Name, StartDate, EndDate, RateUnitOfMeasureId FROM AssetRateCardEntry WHERE AssetId = :assetId ORDER BY UsageResourceId, RateCard.Name ASC ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_UsageUploaderController.cls"
        match: "[ SELECT Id, RateCardEntryId, AdjustmentType, AdjustmentValue, LowerBound, UpperBound, RateUnitOfMeasureName FROM RateAdjustmentByTier WHERE RateCardEntryId IN :rceIds ORDER BY LowerBound ASC ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_UsageUploaderController.cls"
        match: "[SELECT Id, Name FROM UsageResource WHERE Id IN :usageResourceIds]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_UsageUploaderController.cls"
        match: "[ SELECT Id, Name, AssetId, UsageResourceId, EntitlementQuantity, TransactionQuantity, NetQuantity, ChargeForOverage, EffectiveStartDateTime, EffectiveEndDateTime, UsageModelType, GrantType FROM TransactionUsageEntitlement WHERE AssetId = :assetId ORDER BY UsageResourceId ASC ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_UsageUploaderController.cls"
        match: "[SELECT Id, Name FROM UsageResource WHERE Id IN :usageResourceIds]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
      - file: "unpackaged/post_utils/classes/RLM_UsageUploaderController.cls"
        match: "[ SELECT Id, TransactionUsageEntitlementId, BucketBalance, ConsumedEntitlement, TotalConsumedEntitlement, EffectiveStartDateTime, EffectiveEndDateTime FROM UsageEntitlementBucket WHERE TransactionUsageEntitlementId IN :tueIds AND ParentId != NULL ORDER BY EffectiveStartDateTime DESC ]"
        reason: "PRE-EXISTING at registry creation (2026-07-27); NOT adjudicated. Pinned so the gate blocks NEW instances. Each needs review: add WITH USER_MODE, or replace this reason with why system mode is correct."
  - id: apex-soql-dml-in-loop
    description: Governor-limit hazard — SOQL or DML executed inside a loop body.
    source: "AGENTS.md PR Review Focus Areas; audit-review checklist"
    severity: blocker
    detection: baseline
    paths:
      - "force-app/**/classes/*.cls"
      - "unpackaged/**/classes/*.cls"
      - "**/*.trigger"
    patterns:
      - '^\s*for\s*\([^)]*\)\s*\{[^}]*\[\s*SELECT'
      - '^\s*(?:for|while)\s*\([^)]*\)\s*\{[^}]*\b(?:insert|update|delete|upsert)\s+\w'
    allow_list: []
    remediation: Collect into a collection and perform one bulk operation outside the loop.

  - id: formula-vs-apex-case-sensitivity
    description: >
      A comparison whose case sensitivity differs between formula and Apex — formula
      "=" is case-sensitive, Apex String "==" is not.
    source: "audit-review/SKILL.md checklist"
    severity: important
    detection: review-prompt
    paths:
      - "**/*.cls"
      - "**/objects/**/*.field-meta.xml"
      - "**/flows/*.flow-meta.xml"
    patterns: []
    remediation: >
      Use equalsIgnoreCase in Apex only where the formula side is also
      case-insensitive; otherwise make both sides agree explicitly.

  - id: flow-decision-logic
    description: >
      Flow lookup/decision hazards — getFirstRecordOnly assumptions, auto-store vs
      assigned variable, inverted IsNull conditions.
    source: "audit-review/SKILL.md checklist"
    severity: important
    detection: review-prompt
    paths:
      - "**/flows/*.flow-meta.xml"
    patterns: []
    remediation: >
      Confirm each Get Records' storage mode matches how the result is consumed, and
      that IsNull decisions are not inverted.

  - id: feature-gated-metadata-in-always-on-output
    description: >
      Base layouts / flexipages / profiles referencing fields or components that only
      exist behind a feature flag — breaks builds with the flag off.
    source: "audit-review/SKILL.md checklist"
    severity: blocker
    detection: review-prompt
    paths:
      - "templates/**"
      - "unpackaged/post_ux/**"
      - "force-app/**/layouts/**"
    patterns: []
    remediation: >
      Move the reference behind the same flag, or make the component unconditional.
      See repo-integration/ux-assembly-retrieve.md.

  - id: permission-set-grant-mismatch
    description: >
      A permission set that under-grants (feature breaks for non-admins) or
      over-grants (audit finding) relative to what the Apex actually touches in
      user mode.
    source: "audit-review/SKILL.md checklist"
    severity: blocker
    detection: review-prompt
    paths:
      - "**/permissionsets/*.permissionset-meta.xml"
    patterns: []
    remediation: >
      Enumerate every object/field the user-mode code path touches and reconcile
      against the perm set in both directions. See apex-security-hardening/SKILL.md.
